---
name: doc-manual
description: Use when a product manager wants to generate a 产品操作手册 (product operation manual) from a PRD document. Accepts a Feishu document link, local MD/DOCX file path, or requirement name. Generates a structured manual organized by system menu hierarchy, saves to projects/manuals/, and offers Feishu upload.
---

# 产品操作手册生成

根据 PRD 文档，生成面向用户操作指导的产品操作手册。
手册按系统菜单层级组织，每个菜单项下包含功能点、操作步骤、截图占位和注意事项。

## Invocation

```
/doc:manual                          # 无参数，展示输入方式
/doc:manual [飞书文档链接]            # 直接读取飞书 PRD
/doc:manual [本地文件路径]            # 读取本地 PRD 文件（.md / .docx）
```

---

## Step 1: 获取 PRD 文档

**无参数时：**
向 PM 展示选项：
> "请提供 PRD 文档，支持以下方式：
> 1. 飞书文档链接
> 2. 本地 MD 或 DOCX 文件路径"

**传入飞书链接：**

先执行环境预检：

1. 运行 `which lark-cli` 检查是否已安装
   - **未安装** → 停止并告知：
     > "需要安装 lark-cli 才能读取飞书文档。请先运行以下命令完成安装和授权，完成后重新执行：
     > ```
     > brew install lark-cli        # 安装（Mac）
     > lark-cli auth login          # 飞书账号授权
     > ```"

2. 已安装 → 运行 `lark-cli auth status` 检查是否已授权
   - **未授权** → 停止并告知：
     > "lark-cli 已安装但尚未授权。请运行以下命令完成飞书账号授权，完成后重新执行：
     > ```
     > lark-cli auth login
     > ```"

3. 已安装且已授权 → 使用飞书工具读取文档内容。

**传入本地文件路径：**
- `.md` 文件 → 使用 Read 工具读取
- `.docx` 文件 → 使用 `textutil -convert txt -stdout [文件路径]` 转换后读取

---

## Step 2: 识别 PRD 结构

读取 PRD 后，提取以下关键信息：

| 信息 | PRD 位置 | 说明 |
|------|---------|------|
| 系统菜单结构 | 「系统菜单」章节 | 确定手册的菜单分节依据 |
| 功能需求列表 | 「详细功能需求」章节 | 每个功能对应一个或多个菜单项 |
| 通用需求 | 「通用需求」章节 | 版本、变更、审批等横切功能 |
| 权限信息 | 「权限与角色」章节 | 用于各功能的「注意事项」 |

**缺失处理：**
- 「系统菜单」缺失：询问 PM 确认菜单结构后继续
- 其他章节缺失：在对应位置标注 ⚠️ 待补充

---

## Step 3: 确认手册范围

### 3.1 检测已有手册内容

如果目标文件已存在（如 `projects/manuals/xxx-manual.md`），读取并分析已有章节：
- 列出已完成的菜单章节
- 标记哪些菜单尚未编写

### 3.2 逐个确认菜单处理方式

告知 PM 来自 PRD 检测到的菜单列表，然后**依次**确认每个菜单的处理方式：

> "检测到本版本包含 N 个功能菜单，逐个确认处理方式："

**每个菜单的输出格式（一次一条，等待回复后再输出下一条）：**

> **[序号/N] [菜单名称]** （当前状态：已完成 / 未编写）
>
> 请选择处理方式：
> - A：详细编写 — 完整生成功能点、入口、步骤、截图占位、注意事项
> - B：简略 — 只写一段概要说明
> - C：跳过 — 不在手册中体现
> - D：已有 — 保留手册中已有内容不修改

PM 回复后，记录该菜单处理方式，立即输出下一个。

所有菜单确认后，汇总展示一次：

> "已确认处理方式如下，开始生成：
> - [菜单A]：详细编写
> - [菜单B]：简略
> - [菜单C]：跳过
> - [菜单D]：已有，保留"

---

## Step 4: 生成操作手册

### 4.1 模板加载

生成前使用 Read 工具读取模板文件：
- 读取 `~/.gemini/antigravity/skills/prd-to-manual/templates/menu-section.md`

> 模板文件可直接编辑，无需修改 SKILL.md。

### 4.2 PRD 内容提取映射

| 手册元素 | PRD 来源 | 处理方式 |
|---------|---------|---------|
| 菜单名称 | PRD 章节标题（如「基本信息」「MOA」） | 直接使用 |
| ◆ 功能点 | PRD「功能点说明」子节 | 提取每个功能点作为独立 ◆ 子节 |
| 功能入口 | PRD「功能入口」字段 | 使用系统路径格式：`详情页 → 菜单项` |
| 操作步骤 | PRD 交互逻辑、流程描述 | 提炼为 step 1 / step 2 / step 3... |
| 截图位置 | PRD 界面原型 / 交互说明处 | 插入 `[📸 截图：xxx页面]` 占位符 |
| 交互功能点 | PRD 中的字段说明、联动逻辑、自动赋值 | 缩进展示在截图占位符下方 |
| 注意事项 | PRD 权限要求、业务规则、边界条件 | 筛选用户需知的限制条件 |

### 4.3 编写原则

**内容原则：**
- 去掉技术实现细节（如数据库字段、接口参数），保留用户可感知的操作
- 把「系统应当...」改写为直接操作描述（如「点击【变更】按钮」）
- 权限限制统一放在「注意事项」中，格式为：`注意事项：仅 [角色] 可 [操作]`
- 保持客观简洁，不加销售话术

**格式规范：**

每个菜单按如下结构组织（详细模式）：

```markdown
### N.X. 菜单名称

◆ **功能点名称**

功能入口：[路径描述]

[功能概要说明，1-2句]

操作步骤

step 1：[操作描述]
step 2：[操作描述]

[📸 截图：xxx页面]

    交互功能点1：[字段/区域的交互说明]
    交互功能点2：[字段/区域的交互说明]

注意事项：[权限/规则/限制说明]
```

简略模式 （仅用于 B 选项）：

```markdown
### N.X. 菜单名称

[2-3句概要说明该菜单的功能用途]
```

### 4.4 大章分组规则

PRD 中的功能菜单，按所属系统模块分组为大章：

| 大章 | 包含菜单 | 说明 |
|------|---------|------|
| 流程发起与审批 | 立品申请、立项申请、研发决策、变更流程等 | 从 WorkBench 发起的流程操作 |
| Workbench（工作台） | 我的待办/已办/请求 | 个人任务中心 |
| Program（品种） | 基本信息、MOA、临床需求分析、TPP、商业预期、PDP、月度报告、竞品、费用、风险、问题、决策、人员矩阵、相关方等 | 品种详情页左侧菜单 |
| Project（项目） | 基本信息、项目方案、DP/WBS、费用、风险、问题、决策、人员矩阵、相关方等 | 项目详情页左侧菜单 |
| Project_预立项 | 基本信息、MOA、竞品、DP、费用、风险、问题、决策等 | 预立项详情页左侧菜单 |
| Meeting | 查看/新增会议纪要 | 会议管理 |
| 通用操作规范 | 版本管理、草稿管控、高级查询、审批指令 | 横切通用功能 |

章节编号从实际起始编号开始，根据手册已有内容自动递增。

### 4.5 截图处理

- 在需要截图的位置插入占位符：`[📸 截图：xxx页面描述]`
- 截图位置一般放在操作步骤之后
- 截图下方用缩进格式列出交互功能点说明（对应界面中需要标注的区域）
- 文档末尾汇总「截图清单」，PM 对照补充

---

## Step 5: 保存文件并展示摘要

### 5.1 文件保存

**保存路径（相对于当前工作目录根目录）：**
```
projects/manuals/[系统名]-manual.md          # 如 PMIS-manual.md
projects/manuals/[系统名]_[版本名]-manual.md  # 如 PMIS_迭代三-manual.md
```

**追加模式**（目标文件已存在时）：
1. 读取已有内容
2. 找到插入位置（按章节编号顺序）
3. 仅追加新编写的章节，保留已有内容不变
4. 更新文档头部的「版本控制」表格

### 5.2 生成摘要

```
✅ 操作手册已生成/更新

系统：[PMIS]
本次编写菜单：
  - [N.X 菜单A]（详细）
  - [N.Y 菜单B]（简略）
跳过菜单：[菜单C, 菜单D]
保留已有：[菜单E]
待补充截图：N 张（见文档末尾清单）
文件路径：projects/manuals/xxx-manual.md

请确认内容是否符合预期。如需调整请告知。
确认后可帮你上传到飞书。
```

---

## Step 6: 飞书上传（PM 确认后执行）

1. **上传至知识库对应分类目录：**
   - 知识库：YOUR_MANUAL_WIKI_SPACE_URL
   - 在该知识库中找到对应业务分类的目录节点，将文档上传至该节点下

2. 返回飞书文档链接：
   > "✅ 已上传至飞书知识库：[链接]"
